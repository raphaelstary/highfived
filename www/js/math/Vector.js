define(function () {
    /**
     *
     * @param {number} x
     * @param {number} y
     * @constructor
     */
    function Vector(x, y) {
        this.x = x;
        this.y = y;
    }

    return Vector;
});