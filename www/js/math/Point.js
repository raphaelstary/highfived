define(function () {
    /**
     *
     * @param {number} xPoint
     * @param {number} yPoint
     * @constructor
     */
    function Point(xPoint, yPoint) {
        this.xPoint = xPoint;
        this.yPoint = yPoint;
    }

    return Point;
});